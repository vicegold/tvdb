/**
 * Each task has been broken out into its own file in `gulp/tasks`. any file in
 * that folder gets automatically required by the loop in `./gulp/index.js`.
 * to add a new task, simply add a new task file to gulp/tasks.
 *
 * @version 1.4.0
 */
require('./gulp');
